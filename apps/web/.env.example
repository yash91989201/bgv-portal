# Public client/server env — baked into the image at Docker build time.
# Set these as Coolify build args (or compose build args), then rebuild.
VITE_CONVEX_URL=https://your-deployment.convex.cloud
VITE_CONVEX_SITE_URL=https://your-deployment.convex.site
